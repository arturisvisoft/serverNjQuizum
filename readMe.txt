Para hacerlo funcionar solo hay que escribir el siguiente comando:

npm run start

En el archivo route.rest se puden hacer diferentes pruebas de ls llamadas a la BD, hace falta tener instalado
el plugin del visuall studio REST Client